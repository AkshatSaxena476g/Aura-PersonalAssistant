"""AURA application package.

The package is intentionally divided into core, AI, tools, UI, voice, data,
and configuration boundaries so each concern can evolve independently.
"""

__version__ = "0.1.0"
